# 3.6.1 优化建议总览表

3.6.1 优化建议总览表

            表3-12 性能优化建议总览表

         分类   分类描述            优化建议

                   Tiling  提供Tiling相关的优化建议，便于  核间负载均衡
         策略   开发者选择合适的Tiling切分策
             略。

         头尾开  提供降低算子头尾开销（算子执行 设置合适的核数和算子Kernel类型
         销优化  计算前后产生的时延）的优化建
                                           限制TilingData结构大小             议。
                                  避免TPipe在对象内创建和初始化

                                     核函数内删除Workspace相关冗余
                              操作

         流水编  通过任务并行化、异步调度等方    使能DoubleBuffer
         排    法，提升硬件资源利用率，实现更
                                               使能Iterate或IterateAll异步接口避             高的吞吐率。
                                         免AIC/AIV同步依赖

         内存访  通过控制搬运的数据块大小和GM  尽量一次搬运较大的数据块
         问    地址等来实现搬运效率的最大化；
                                   GM地址尽量512B对齐                通过Buffer的共享与复用、数据压
             缩精简、使用专用存储空间、访存                                  高效的使用搬运API
             调度优化等方法来减少内存占用，
             提升计算效率。         避免同地址访问

                                  设置合理的L2 CacheMode

                                       算子与高阶API共享临时Buffer

                                      纯搬运类算子VECIN和VECOUT建
                              议复用

                                         通过缩减Tensor ShapeInfo维度，
                              优化栈空间

                                                避免Unified Buffer的bank冲突

                                                     L2 Cache切分

         矢量计  矢量计算相关优化建议。         通过Unified Buffer融合实现连续
         算                                vector计算

                                          Vector算子灵活运用Counter模式

                              选择低延迟指令，优化归约操作性
                              能

         矩阵计  矩阵计算相关优化建议。      通过BT Buffer实现高效的bias计算
         算
                                      通过FP Buffer存放量化参数实现高
                              效随路量化

分类   分类描述            优化建议

                                        通过L0C Buffer数据暂存实现高效
                              的矩阵乘结果累加

                                 较小矩阵长驻L1 Buffer，仅分次搬
                              运较大矩阵

                                           Matmul使能AtomicAdd选项


3.6.2 Tiling 策略
