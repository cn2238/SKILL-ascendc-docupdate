# 3.7.4.1 Matmul性能优化策略总览

3.7.4.1 Matmul 性能优化策略总览

         本节提供了一系列包含Matmul计算的算子性能调优案例，开发者可根据实际应用场
        景，参考相关案例中的优化方法和思路，应用于具体实践中。案例分为如下五类，各
        分类的简介请参见如下表格，详细内容请阅读后续章节。

             ●   Tiling优化

               表3-19 Tiling 优化策略总览

           分类         适用场景           相关案例

                Tiling优化：优化    数据量足够多的大Shape场    Matmul算子优化
               Tiling分核及基本块切 景。                     Tiling策略
           分策略。


             ● 并行度优化

               表3-20 并行度优化策略总览

           分类         适用场景           相关案例

           多核间任务并行：合  矩阵的K轴较大、M轴和N轴相  Matmul高阶API使
           理地将数据分配给不  比K轴较小的场景。      能多核切K
           同的核来执行任务。

           多核间数据访问并   多核执行Matmul，输入矩阵   Matmul高阶API使
           行：优化多核数据并  的K轴较大且K轴非全载的场  能多核K轴错峰访问
           行访问机制，如多核  景。             内存
           场景同一内存数据的
           地址访问冲突优化，
           实现多核数据访问效
           率提升。

           单核内流水并行：利    1. 算子的MMAD流水和          1. Matmul高阶API
           用不同指令队列间的    FIXPIPE流水之间串行执       使能UnitFlag
           相互独立性和可并行   行，同步等待的时间在算子   2. Matmul高阶API
           执行特性，优化核内   整体执行耗时中占比较高。    使能NBuffer33模
           流水并行度。            2. MTE2 Bound且MTE2流水   板
                       和其他流水串行执行。


             ● 内存优化

表3-21 内存优化策略总览

   分类         适用场景           相关案例

   内存共享与复用：通  MIX场景下，多个AIV的A矩阵  Matmul高阶API使
   过Buffer的共享与缓  或B矩阵GM地址相同，且多个  能IBShare模板共享A
   存复用，减少重复的  AIV复用的A矩阵或B矩阵在L1  和B矩阵数据
   数据搬运带来的开    Buffer上全载。                                        Matmul高阶API使
   销。                                 能IBShare模板共享B
                            矩阵数据

   内存对齐：确保处理  输入矩阵内轴非256字节对    AIV核上的ND2NZ格
   的数据满足特定的对  齐，且数据量较大的场景。   式转换
   齐要求，针对非对齐
   数据使用不同的搬运
   策略，以提升数据搬
   运的效率。


●  Scalar优化

   表3-22 Scalar 优化策略总览

   分类         适用场景           相关案例

    Tiling常量化：在     Matmul初始化时的Scalar计算  Matmul高阶API使
   Kernel编译期间完成  较多，影响指令头开销。     能Tiling全量常量化
    Matmul Tiling的计                   Matmul迭代之间的Scalar计算
   算，由变量转化为常  较多，阻塞MTE2流水。
   量扩散到系统中，减
   少Scalar提升性能。

   纯Cube模式：减少消  相较于MIX模式，没有矢量计   Matmul高阶API使
   息处理机制带来额外  算，只有矩阵计算的场景。   能纯Cube模式
    的Scalar开销。


● 搬运优化

   表3-23 搬运优化策略总览

   分类         适用场景           相关案例

   搬运吞吐量优化：通    1. MTE2循环搬运次数多的大    1. Matmul 高阶API
   过合理控制搬运数据    shape场景。          使能MDL模板
   块的大小，提升带宽    2. 输入和输出的数据量超过L2   2. Matmul高阶API
   利用效率，实现搬运    Cache大小的场景。        使能L2 Cache切
   效率的提升。                             分

   预加载搬运：预加载  MTE2流水间隙较大，且M或N  Matmul高阶API使
   需要搬运的数据块，  数值较大的场景。         能MTE2 Preload
   减少流水之间的间
   隙。
