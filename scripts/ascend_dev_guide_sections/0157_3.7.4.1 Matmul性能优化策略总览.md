## Matmul性能优化策略总览

本节提供了一系列包含Matmul计算的算子性能调优案例，开发者可根据实际应用场景，参考相关案例中的优化方法和思路，应用于具体实践中。案例分类及简介请参见下表，详细内容请阅读后续章节。

表3-19 Matmul 性能优化策略总览表

| 分类 | 子分类 | 适用场景 | 相关案例 |
| --- | --- | --- | --- |
| Tiling | Tiling优化：优化数据量足够多的大Shape场 | ●Matmul算子优优化 | Tiling分核及基本块景。 化Tiling策略切分策略。 |
| Tiling | 并行多核间任务并行：合矩阵的K轴较大、M轴和N轴 | ●Matmul高阶API度优理地将数据分配给不相比K轴较小的场景。 | 使能多核切K化同的核来执行任务。 |
| Tiling | 多核间数据访问并多核执行Matmul，输入矩阵 | ●Matmul高阶API行：优化多核数据并的K轴较大且K轴非全载的场使能多核K轴错行访问机制，如多核景。 | 峰访问内存<br>场景同一内存数据的<br>地址访问冲突优化，<br>实现多核数据访问效 |

率提升。

单核内流水并行：利算子的MMAD流水和FIXPIPE ●Matmul高阶API用不同指令队列间的流水之间串行执行，同步等使能UnitFlag相互独立性和可并行待的时间在算子整体执行耗执行特性，优化核内时中占比较高。流水并行度。

MTE2 Bound且MTE2流水和 ●Matmul高阶API其他流水串行执行。 使能NBuffer33

### 模板

内存内存共享与复用：通 MIX场景下，多个AIV的A矩 ●Matmul高阶API优化过Buffer的共享与缓阵或B矩阵GM地址相同，且使能IBShare模存复用，减少重复的多个AIV复用的A矩阵或B矩阵板共享A和B矩阵数据搬运带来的开在L1 Buffer上全载。 数据销。 ●Matmul高阶API使能IBShare模板共享B矩阵数据

### 分类 子分类 适用场景 相关案例

内存对齐：确保处理输入矩阵内轴非256字节对 ●AIV核上的的数据满足特定的对齐，且数据量较大的场景。 ND2NZ格式转换齐要求，针对非对齐数据使用不同的搬运策略，以提升数据搬运的效率。

Scala Tiling常量化：在 ●Matmul初始化时的Scalar ●Matmul高阶API r优化 Kernel编译期间完成计算较多，影响指令头开使能Tiling全量Matmul Tiling的计销。 常量化算，由变量转化为常 ●Matmul迭代之间的Scalar量扩散到系统中，减计算较多，阻塞MTE2流少Scalar提升性能。 水。

纯Cube模式：减少相较于MIX模式，没有矢量计 ●Matmul高阶API消息处理机制带来额算，只有矩阵计算的场景。 使能纯Cube模式外的Scalar开销。

搬运搬运吞吐量优化：通 MTE2循环搬运次数多的大 ●Matmul 高阶优化过合理控制搬运数据 shape场景。 API使能MDL模块的大小，提升带宽板利用效率，实现搬运效率的提升。 输入和输出的数据量超过L2 ●Matmul高阶API Cache大小的场景。 使能L2 Cache切分

预加载搬运：预加载 MTE2流水间隙较大，且M或 ●Matmul高阶API需要搬运的数据块， N数值较大的场景。 使能MTE2减少流水之间的间 Preload隙。
