## SoftMax/SimpleSoftMax Tiling

### 功能说明

用于获取SoftMax/SimpleSoftMax Tiling参数。

### 函数原型

- 获取Kernel接口计算所需最大/最小临时空间的接口
```cpp
  uint32_t GetSoftMaxMaxTmpSize(const ge::Shape& srcShape, const uint32_t dataTypeSize, const bool
isReuseSource)
  uint32_t GetSoftMaxMinTmpSize(const ge::Shape& srcShape, const uint32_t dataTypeSize, const bool
isReuseSource)
```

- Tiling计算接口
- AscendC::optiling命名空间下的计算接口
```cpp
 void SoftMaxTilingFunc(const ge::Shape& srcShape, const uint32_t dataTypeSize, const uint32_t
localWorkSpaceSize, optiling::SoftMaxTiling& softmaxTiling)
```

- AscendC命名空间下的计算接口
```cpp
 void SoftMaxTilingFunc(const ge::Shape& srcShape, const uint32_t dataTypeSize, const uint32_t
localWorkSpaceSize, AscendC::tiling::SoftMaxTiling& softmaxTiling)
```

### 参数说明

表4-713 SoftMax/SimpleSoftMax GetSoftMaxMaxTmpSize/

| GetSoftMaxMinTmpSize | 接口参数列表 |
| --- | --- |
| 接口 输入/ | 功能<br>输出 |
| srcShape | 输入输入srcTensor的shape信息。 |
| dataTypeSi | 输入参与计算的max和sum的数据类型，比如half=2。ze |
| isReuseSou | 输入与kernel侧接口配置保持一致。rce |

表4-714 SoftMax/SimpleSoftMax SoftMaxTilingFunc 接口参数列表

### 接口 输入/ 功能

### 输出

srcShape 输入输入srcTensor的shape信息。

dataTypeSi 输入参与计算的max和sum的数据类型，比如half=2。ze

localWorkS 输入剩余的可供SoftMax接口计算的空间大小，单位为Byte。paceSize localWorkSpaceSize的取值必须大于GetSoftMaxMinTmpSize接口返回的计算所需的最小临时空间大小。接口输入/ 功能

### 输出

softmaxTili 输出输出SoftMax接口所需的tiling信息，支持ng optiling::SoftMaxTiling形式入参和AscendC::tiling::SoftMaxTiling形式入参。

### 返回值说明

GetSoftMaxMaxTmpSize返回SoftMax/SimpleSoftMax接口能完成计算所需最大临时空间大小，单位为Byte。GetSoftMaxMinTmpSize返回SoftMax/SimpleSoftMax接口能完成计算所需最小临时空间大小，单位为Byte。

### 约束说明

### 无
