## MatmulCallBackFunc

### 产品支持情况

### 产品 是否支持

| 产品 | 是否支持 |
| --- | --- |
| Atlas A3 训练系列产品/Atlas A3 推理系列产品 | √ |
| Atlas A2 训练系列产品/Atlas A2 推理系列产品 | √ |
| Atlas 200I/500 A2 推理产品 | x |
| Atlas 推理系列产品AI Core | x |
| Atlas 推理系列产品Vector Core | x |
| Atlas 训练系列产品 | x |

### 功能说明

模板参数MatmulCallBackFunc支持用户定制化Matmul的A矩阵、B矩阵及C矩阵的搬入搬出功能，如非连续搬入或针对搬出设置不同的数据片段间隔等。具体方式为：用户根据实际需要，实现一个或多个自定义的搬运函数，定义Matmul对象时，通过模板参数MatmulCallBackFunc，传入实现的搬运函数的函数指针，传入的函数指针会替换Matmul流程中默认的搬运函数。MatmulCallBackFunc中包含3个可由用户自定义的回调函数接口，即用户可配置3个函数指针。3个函数指针分别为C矩阵从CO1拷贝到GM、A矩阵从GM拷贝到A1、B矩阵从GM拷贝到B1的回调函数指针。3个函数指针的位置固定，不使用自定义搬运函数的函数指针位置需要设为空指针。各个功能回调函数接口定义及参数释义见表1 MatmulCallBackFunc回调函数接口及参数说明。每个回调函数实现矩阵搬运中单个基本块（A矩阵基本块baseM * baseK、B矩阵基本块baseK * baseN、C矩阵基本块baseM * baseN）的搬运策略，无法对整块内存空间进行管理。Matmul默认的搬运函数实现单核上单个基本块的搬运，搬运的基本块大小是固定的，在完整的Matmul计算过程中，多次调用搬运函数，对连续排布的基本块按顺序逐个搬运，以搬入A矩阵的过程为例，示意图如下。

图4-40 Matmul 默认搬入A 矩阵示意图

表4-606 MatmulCallBackFunc 回调函数接口及参数说明

### 回调函数 回调函数接口 参数说明

功能

可自定义 void DataCopyOut(const gm：输出的GM地址。设置不同 `__gm__` void *gm, const co1Local: CO1上的计算结果。的搬出数 LocalTensor<int8_t>dataCopyOutParams：Matmul定义的据片段数 &co1Local, const void DataCopyOutParams结构体指针，供用目等参 *dataCopyOutParams, const户参考使用。 数，实现 uint64_t tilingPtr, const struct DataCopyOutParams {将 uint64_t dataPtr) uint16_t cBurstNum; //传输数据片段数目Matmul uint16_t burstLen; //连续传输数据片段长度计算结果 uint16_t srcStride;//源tensor相邻连续数据片段间隔uint32_t dstStride; // 目的tensor相邻连续数据片段从CO1搬间隔出到GM uint16_t oriNSize; // NZ转ND时，源tensorN方向大的功能小bool enUnitFlag; // 是否使能UnitFlag uint64_t quantScalar; // 量化场景下量化Scalar的值uint64_t cbufWorkspaceAddr; //量化场景下量化Tensor地址

```cpp
}
```

tilingPtr: 用户使用SetUserDefInfo设置的tiling参数地址。

dataPtr: 用户使用SetSelfDefineData设置的计算数据地址。

### 回调函数 回调函数接口 参数说明

功能

可自定义 void CopyA1(const aMatrix: 目标L1Buffer地址。左矩阵搬 LocalTensor<int8_t> gm：左矩阵GM首地址。入首地 &aMatrix, const `__gm__`row、col：搬运块在M、K方向的索引， 址、搬运 void *gm, int row, int col,即在M、K方向上搬运块的序号，序号从块位置、 int useM, int useK, const 0开始。 搬运块大 uint64_t tilingPtr, const小，实现 uint64_t dataPtr) useM、useK：搬运块M、K方向大小，左矩阵从单位为元素个数。通过row、col和GM搬入 useM、useK计算出该搬运块左上角在左L1的功能矩阵中的地址偏移。

tilingPtr: 用户使用SetUserDefInfo设置的tiling参数地址。

dataPtr: 用户使用SetSelfDefineData设置的计算数据地址。

可自定义 void CopyB1(const bMatrix: 目标L1Buffer地址。右矩阵搬 LocalTensor<int8_t> gm：右矩阵GM首地址。入首地 &bMatrix, const `__gm__`row、col：搬运块在K、N方向的索引， 址、搬运 void *gm, int row, int col,即在K、N方向上搬运块的序号，序号从块位置、 int useK, int useN, const 0开始。 搬运块大 uint64_t tilingPtr, const小，实现 uint64_t dataPtr) useK、useN：搬运块K、N方向大小，右矩阵从单位为元素个数。通过row、col和GM搬入 useK、useN计算出该搬运块左上角在右L1的功能矩阵中的地址偏移。

tilingPtr: 用户使用SetUserDefInfo设置的tiling参数地址。

dataPtr: 用户使用SetSelfDefineData设置的计算数据地址。

### 约束说明

### 无

### 调用示例

完整的使用样例请参考Matmul回调函数样例。

```cpp
//用户自定义回调函数
          void DataCopyOut(const __gm__ void *gm, const LocalTensor<int8_t> &co1Local, const void
```

- dataCopyOutParams, const uint64_t tilingPtr, const uint64_t dataPtr);
```cpp
   void CopyA1(const LocalTensor<int8_t> &aMatrix, const __gm__ void *gm, int row, int col, int useM, int
   useK, const uint64_t tilingPtr, const uint64_t dataPtr);
   void CopyB1(const LocalTensor<int8_t> &bMatrix, const __gm__ void *gm, int row, int col, int useK, int useN,
   const uint64_t tilingPtr, const uint64_t dataPtr);

  AscendC::Matmul<aType, bType, cType, biasType, CFG_NORM, MatmulCallBackFunc<DataCopyOut, CopyA1,
CopyB1>> mm;
REGIST_MATMUL_OBJ(&pipe, GetSysWorkSpacePtr(), mm, &tiling);
    uint64_t tilingPtr = reinterpret_cast<uint64_t>(tiling);
    mm.SetUserDefInfo(tilingPtr);
  GlobalTensor<SrcT> dataGM; // 保存有回调函数需使用的计算数据的GM
    uint64_t dataGMPtr = reinterpret_cast<uint64_t>(dataGM.address_);
  mm.SetSelfDefineData(dataGMPtr);
 mm.SetTensorA(gmA);
 mm.SetTensorB(gmB);
    mm.IterateAll();
```
