# 2.6.3.2 NPU架构版本220x

2.6.3.2 NPU 架构版本220x

        本节介绍硬件约束以及解决方案建议。对应的产品型号为：

             ●   Atlas A3 训练系列产品/Atlas A3 推理系列产品
             ●   Atlas A2 训练系列产品/Atlas A2 推理系列产品

表2-19 硬件约束以及解决方案建议

 分类   硬件约束描述       解决方案建议

 内存访问 各存储单元的最小访问粒  ●进行数据搬运时，需要感知对齐约束。
 （L0   度/地址对齐要求：                   ●针对UB，遇到一些非对齐的场景，可以
 Buffer/L            Unified Buffer：32Byte对  使用非对齐搬运的接口或者通过一些技
 1      齐。            巧（比如搬入时包含冗余数据，搬出时
 Buffer/U           L1 Buffer：32Byte对齐。   去除冗余数据）来解决。详情见3.3.2.7 B等）                    非对齐场景。          L0A Buffer/L0B Buffer：
        512Byte对齐。
          L0C Buffer：64Byte对
      齐。
            BiasTable Buffer：64Byte
      对齐。
            Fixpipe Buffer：128Byte
      对齐。

 内存访问  UB bank访问冲突     需要按照芯片要求，在软件实现时错开处理
 （UB）  （Vector计算访问/搬运访 的地址，从而解决bank冲突。具体解决方
      问）。               案可参考3.6.5.9 避免Unified Buffer的
                      bank冲突章节。

 内存访问 多核并行同地址访问    对相同地址的访问，会被硬件串行化，性能
 （GM）  GM，会被硬件串行化。  为排队时间，大约下降10%-20%；
                  多核访问通过错峰访问（调整数据访问顺序
                  和修改切分策略等），使得第一次加载数据
                           到L2 Cache，后续访问性能反而提升。具
                         体解决方案可参考3.6.5.4 避免同地址访问
                  章节。

 内存访问 单次搬运数据长度16KB以 根据实测经验，单次搬运数据长度16KB以
 （GM）  上时，可发挥带宽的最佳  上时，通常能较好地发挥出带宽的最佳性
      性能。          能。因此对于单次搬运，应考虑尽可能的搬
                  运较大的数据块（不同芯片不一样）。
                        具体解决方案可参考3.6.5.1 尽量一次搬运
                  较大的数据块章节。

 内存访问 DataCopy中源操作数相邻 前面一个数据块的尾与后面数据块的头的间
 （GM--  连续数据块的间隔（前面  隔超出65535时，需要拆分成多条指令来实
 >L1）  一个数据块的尾与后面数  现。
      据块的头的间隔）不要超
       出65535，单位为
        DataBlock（32字节）。

 内存访问 数据搬运会被拆成128B/   Tiling尽量让搬运的内轴128B、256B、
 （GM）  256B/512B不同长度进行  512B对齐。
      搬运，非对齐会向上取   具体解决方案可参考3.6.5.2 GM地址尽量
      整。             512B对齐章节。

分类   硬件约束描述       解决方案建议

Cube   MTE1和MMAD指令队列  对应指令队列容易满，会阻塞其他指令下
      的深度为32。       发，引起流水断流。
                             Load2D从L1 Buffer搬运到L0 Buffer，需要
                     发射32条指令，Load3D只需要一条指令，
                      建议使用Load3D来实现。

ICache   ICache硬件规格限制     拆分Tiling_key或使用模板函数来减少代码
        32KB。               段。详情见2.10.2.5.5 Tiling模板编程。

ICache  多核并行同地址访问    小Shape场景尽量减少启动核数，减少多核
       ICache，会被硬件串行   同地址访问问题。
     化。

DCache  DCache硬件规格限制   无
         32KB

Scalar  标量写GM时，数据会被   使用DataCacheCleanAndInvalid来保证一
      缓存在Dcache。硬件不保 致性。
      证DCache和GM一致性，
     需要用户保证。

Cube     L0C Buffer容量128KB。  无

Cube      BiasTable Buffer为1KB。  无

Cube    Cube计算场景float算力为 无
        half算力的1/4。

Cube   Cube输出随路量化场景，  ●AIV上存在int32_t->float、
        不支持int32_t到            float>bfloat16_t的转换。
        bfloat16_t类型量化。                            ●AIC支持float->bfloat16_t的随路量化。

Vector    Reduce接口half比float性  half写回UB时存在非32Byte对齐，导致性
     能差。            能劣化，建议把half转float计算，此场景建
                       议使用float数据类型。

Vector   Exp/Ln接口处理同样数量  内部对float做了优化，故两者性能相当，
        的half/float的耗时是一样 开发者可以根据实际情况选择合适的精度类
     的            型。

流水同步  set/wait同步不匹配，状   通过孪生调试/mssanitizer工具，提前识别
（核内） 态会残留，影响后续算   此类问题。
     子。

流水同步  CrossCoreSetFlag计数器  通过孪生调试/mssanitizer工具，超出场景
（核间） 存在限制，超出15次需要  提前报错。
     做一次反向同步，否则会
     出现卡死。

分类   硬件约束描述       解决方案建议

            API通用   使用Ascend C API时，源  使用基础API的Tensor高维切分计算接口
         约束   操作数和目的操作数的地  时，为了节省地址空间，开发者可以定义一
              址重叠通用约束       个Tensor，供源操作数与目的操作数同时使
                          用（即地址重叠）。使用时需要注意以下约
                          束：
                          ●单次迭代内：源操作数与目的操作数必
                              须100%完全重叠，不支持部分重叠。
                          ●多次迭代间：不支持前序迭代的目的操
                            作数与后序迭代的源操作数重叠。例
                              如，第N次迭代的目的操作数是第N+1
                            次的源操作数。在这种情况下，第N次
                            迭代可能会改写覆盖源操作数的数值，
                            导致无法得到预期结果。特别地，对于
                                   部分双目计算类的API（Add、Sub、
                                          Mul、Max、Min、AddRelu、
                                      SubRelu），当数据类型为half、
                                       int32_t、float时，支持前序迭代的目的
                            操作数与后序迭代的源操作数重叠：仅
                            针对目的操作数和第二个源操作数重叠
                                          的情况，且src1RepStride或者
                                              dstRepStride必须为0。


2.7 调试调优
